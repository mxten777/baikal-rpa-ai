# BAIKAL RPA AI — 관리자 매뉴얼

> **문서 버전**: 1.0  
> **작성일**: 2026-02-27  
> **작성자**: BAIKAL AI 개발팀  
> **대상 독자**: 시스템 관리자, DevOps 엔지니어

---

## 목차

1. [시스템 요구사항](#1-시스템-요구사항)
2. [설치 가이드](#2-설치-가이드)
3. [환경 설정](#3-환경-설정)
4. [서버 기동 및 중지](#4-서버-기동-및-중지)
5. [사용자 관리](#5-사용자-관리)
6. [AI 엔진 설정](#6-ai-엔진-설정)
7. [데이터베이스 관리](#7-데이터베이스-관리)
8. [보안 관리](#8-보안-관리)
9. [모니터링 및 헬스체크](#9-모니터링-및-헬스체크)
10. [백업 및 복구](#10-백업-및-복구)
11. [트러블슈팅](#11-트러블슈팅)
12. [운영 체크리스트](#12-운영-체크리스트)

---

## 1. 시스템 요구사항

### 1.1 로컬 개발 환경

| 항목 | 최소 사양 | 권장 사양 |
|------|-----------|-----------|
| **OS** | Windows 10 / macOS 12 / Ubuntu 20.04 | Windows 11 / macOS 14 / Ubuntu 22.04 |
| **CPU** | 2코어 | 4코어 이상 |
| **RAM** | 4GB | 8GB 이상 |
| **디스크** | 5GB 여유 | 20GB 이상 |
| **Python** | 3.11 이상 | 3.11.x |
| **Node.js** | 18 이상 | 20 LTS 이상 |
| **네트워크** | OpenAI API 접근 가능 | - |

### 1.2 Docker 운영 환경

| 항목 | 최소 사양 | 권장 사양 |
|------|-----------|-----------|
| **Docker** | 24.0 이상 | 최신 안정 버전 |
| **Docker Compose** | v2.0 이상 | 최신 안정 버전 |
| **CPU** | 4코어 | 8코어 이상 |
| **RAM** | 8GB | 16GB 이상 |
| **디스크** | 20GB | 50GB 이상 (SSD 권장) |

---

## 2. 설치 가이드

### 2.1 로컬 개발 환경 설치

#### Step 1: 소스코드 다운로드

```bash
git clone https://github.com/baikal-ai/baikal-rpa-ai.git
cd baikal-rpa-ai
```

#### Step 2: 환경 변수 파일 생성

```bash
# .env 파일 복사 및 수정
cp .env.example .env

# 백엔드용 .env도 복사
cp .env backend/.env
```

#### Step 3: 백엔드 설치

```bash
# Python 가상환경 생성 (권장)
cd backend
python -m venv .venv

# 가상환경 활성화
# Windows:
.venv\Scripts\activate
# macOS/Linux:
source .venv/bin/activate

# 의존성 설치
pip install -r requirements.txt

# aiosqlite 설치 (로컬 개발용)
pip install aiosqlite
```

#### Step 4: 프론트엔드 설치

```bash
cd ../frontend
npm install
```

#### Step 5: Playwright 브라우저 설치 (RPA 웹 스크래핑 사용 시)

```bash
python -m playwright install chromium
```

### 2.2 Docker 환경 설치

```bash
# 소스코드 다운로드
git clone https://github.com/baikal-ai/baikal-rpa-ai.git
cd baikal-rpa-ai

# 환경 변수 설정
cp .env.example .env
# .env 파일 편집 (아래 '환경 설정' 섹션 참고)

# Docker Compose로 전체 서비스 기동
docker-compose up -d --build
```

---

## 3. 환경 설정

### 3.1 `.env` 파일 필수 설정

`.env` 파일을 열어 아래 항목들을 반드시 수정하세요:

```dotenv
# ===== 필수 변경 항목 =====

# 1. JWT 비밀키 — 반드시 랜덤 문자열로 변경
JWT_SECRET=여기에-랜덤-시크릿키-입력-32자이상

# 2. AI 제공자 선택 (openai 또는 ollama)
AI_PROVIDER=openai

# 3. OpenAI API 키 (AI_PROVIDER=openai인 경우)
OPENAI_API_KEY=sk-your-actual-api-key-here

# 4. 사용할 모델 (비용/성능 트레이드오프)
OPENAI_MODEL=gpt-3.5-turbo
# 고품질 응답이 필요한 경우:
# OPENAI_MODEL=gpt-4
```

### 3.2 로컬 개발용 설정

```dotenv
# 로컬 SQLite 사용
DATABASE_URL=sqlite+aiosqlite:///./baikal_rpa.db

# CORS 허용
CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]
```

### 3.3 Docker 운영용 설정

```dotenv
# PostgreSQL 사용
POSTGRES_USER=baikal
POSTGRES_PASSWORD=매우안전한비밀번호
POSTGRES_DB=baikal_rpa
DATABASE_URL=postgresql+asyncpg://baikal:매우안전한비밀번호@postgres:5432/baikal_rpa

# Redis
REDIS_URL=redis://redis:6379/0

# 운영 도메인 CORS
CORS_ORIGINS=["https://rpa.your-domain.com"]
```

### 3.4 JWT 비밀키 생성 방법

```bash
# Python으로 안전한 랜덤 키 생성
python -c "import secrets; print(secrets.token_urlsafe(64))"
```

> ⚠️ **주의**: 기본값 `change-me`를 절대 운영 환경에서 사용하지 마세요.

---

## 4. 서버 기동 및 중지

### 4.1 로컬 개발 환경

#### 백엔드 서버 시작

```bash
cd backend
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

- **접속**: `http://localhost:8000`
- **API 문서**: `http://localhost:8000/docs` (Swagger UI)
- **ReDoc**: `http://localhost:8000/redoc`
- `--reload`: 코드 변경 시 자동 재시작 (개발용)

#### 프론트엔드 서버 시작

```bash
cd frontend
npm run dev
```

- **접속**: `http://localhost:5173`
- HMR(Hot Module Replacement) 자동 활성화

#### 서버 중지

```bash
# 해당 터미널에서 Ctrl+C
```

### 4.2 Docker 환경

#### 전체 서비스 시작

```bash
docker-compose up -d
```

#### 상태 확인

```bash
docker-compose ps
```

#### 로그 확인

```bash
# 전체 로그
docker-compose logs -f

# 특정 서비스 로그
docker-compose logs -f api
docker-compose logs -f worker
```

#### 서비스 중지

```bash
docker-compose down
```

#### 서비스 중지 + 데이터 삭제

```bash
docker-compose down -v   # 볼륨까지 삭제 (주의: DB 데이터 소실)
```

#### 개별 서비스 재시작

```bash
docker-compose restart api
docker-compose restart worker
```

---

## 5. 사용자 관리

### 5.1 최초 관리자 계정 생성

서버 최초 기동 후, API를 통해 관리자 계정을 등록합니다:

```bash
# Windows PowerShell
$body = '{"email":"admin@baikal.ai","password":"admin1234","name":"관리자"}'
Invoke-RestMethod -Uri http://localhost:8000/auth/register `
  -Method POST -Body $body -ContentType "application/json"

# macOS/Linux (curl)
curl -X POST http://localhost:8000/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@baikal.ai","password":"admin1234","name":"관리자"}'
```

> ⚠️ 등록 후 반드시 비밀번호를 변경하세요.

### 5.2 사용자 역할

| 역할 | 설명 | 권한 |
|------|------|------|
| `user` | 일반 사용자 | 자신의 데이터에 대한 CRUD |
| `admin` | 관리자 | 전체 데이터 접근, 시스템 설정 |

### 5.3 사용자 현황 확인

```bash
# SQLite (로컬)
sqlite3 backend/baikal_rpa.db "SELECT id, email, name, role, created_at FROM users;"

# PostgreSQL (Docker)
docker-compose exec postgres psql -U baikal -d baikal_rpa \
  -c "SELECT id, email, name, role, created_at FROM users;"
```

### 5.4 사용자 역할 변경

```bash
# SQLite
sqlite3 backend/baikal_rpa.db \
  "UPDATE users SET role='admin' WHERE email='admin@baikal.ai';"

# PostgreSQL
docker-compose exec postgres psql -U baikal -d baikal_rpa \
  -c "UPDATE users SET role='admin' WHERE email='admin@baikal.ai';"
```

### 5.5 사용자 비밀번호 초기화

비밀번호는 bcrypt로 해시되어 저장되므로, 직접 DB에서 변경하려면 Python 스크립트를 사용합니다:

```python
from passlib.context import CryptContext
pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")
new_hash = pwd_context.hash("newPassword123")
print(new_hash)
# 출력된 해시를 DB의 password_hash 컬럼에 UPDATE
```

---

## 6. AI 엔진 설정

### 6.1 OpenAI (클라우드 AI)

**장점**: 높은 품질, 다양한 모델, 빠른 응답  
**단점**: API 비용 발생, 데이터 외부 전송

```dotenv
AI_PROVIDER=openai
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_MODEL=gpt-3.5-turbo   # 또는 gpt-4, gpt-4o
```

**모델 선택 가이드:**

| 모델 | 비용 | 품질 | 속도 | 권장 용도 |
|------|------|------|------|-----------|
| gpt-3.5-turbo | 낮음 | 양호 | 빠름 | 일반 대화, 이메일 |
| gpt-4 | 높음 | 우수 | 보통 | 보고서, 공문 |
| gpt-4o | 중간 | 우수 | 빠름 | 범용 |

### 6.2 Ollama (로컬 AI)

**장점**: 무료, 데이터 외부 미전송 (보안)  
**단점**: GPU 필요, 상대적으로 낮은 품질

```dotenv
AI_PROVIDER=ollama
OLLAMA_BASE_URL=http://localhost:11434    # 로컬 Ollama
OLLAMA_MODEL=llama3
```

**Ollama 설치 및 모델 다운로드:**

```bash
# Ollama 설치 (https://ollama.com)
# 모델 다운로드
ollama pull llama3
ollama pull mistral    # 대안 모델

# Ollama 서버 시작
ollama serve
```

### 6.3 AI 제공자 전환

`.env` 파일에서 `AI_PROVIDER` 값만 변경하면 즉시 전환됩니다. 서버 재시작이 필요합니다.

```dotenv
# OpenAI → Ollama 전환
AI_PROVIDER=ollama
```

---

## 7. 데이터베이스 관리

### 7.1 SQLite (로컬 개발)

**DB 파일 위치:**
```
backend/baikal_rpa.db
```

**SQLite 접속:**
```bash
sqlite3 backend/baikal_rpa.db
```

**주요 조회 명령:**
```sql
-- 테이블 목록
.tables

-- 테이블 구조 조회
.schema users
.schema documents
.schema automations
.schema automation_runs

-- 사용자 수 확인
SELECT COUNT(*) FROM users;

-- 문서 수 확인
SELECT COUNT(*) FROM documents;

-- 자동화 수 확인
SELECT COUNT(*) FROM automations;

-- 최근 실행 기록
SELECT id, automation_id, status, started_at, finished_at
FROM automation_runs ORDER BY started_at DESC LIMIT 10;
```

### 7.2 PostgreSQL (Docker)

**접속:**
```bash
docker-compose exec postgres psql -U baikal -d baikal_rpa
```

**테이블 목록:**
```sql
\dt
```

### 7.3 데이터 초기화

> ⚠️ **주의**: 모든 데이터가 삭제됩니다.

```bash
# SQLite — DB 파일 삭제 후 서버 재시작
rm backend/baikal_rpa.db
# 서버 재시작하면 자동으로 테이블 재생성

# PostgreSQL (Docker)
docker-compose down -v
docker-compose up -d
```

### 7.4 테이블 자동 생성

서버 시작 시 `lifespan` 이벤트에서 `Base.metadata.create_all()`이 자동 실행되어 누락된 테이블이 생성됩니다.

---

## 8. 보안 관리

### 8.1 필수 보안 설정

| 항목 | 상태 | 조치 |
|------|------|------|
| JWT_SECRET 변경 | ⬜ | 64자 이상 랜덤 문자열로 변경 |
| HTTPS 적용 | ⬜ | Nginx + Let's Encrypt SSL/TLS |
| CORS 제한 | ⬜ | 운영 도메인만 허용 |
| DB 비밀번호 변경 | ⬜ | POSTGRES_PASSWORD 강화 |
| API 키 보호 | ⬜ | .env 파일 권한 600 설정 |

### 8.2 JWT 토큰 관리

```dotenv
# 토큰 만료 시간 조절 (분 단위)
JWT_EXPIRE_MINUTES=480    # 8시간 (기본)
JWT_EXPIRE_MINUTES=60     # 1시간 (보안 강화)
JWT_EXPIRE_MINUTES=1440   # 24시간 (편의성 우선)
```

### 8.3 CORS 설정

```dotenv
# 개발 환경
CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]

# 운영 환경 (도메인 제한)
CORS_ORIGINS=["https://rpa.baikal.ai"]
```

### 8.4 `.env` 파일 보호

```bash
# Linux/macOS 권한 설정
chmod 600 .env
chmod 600 backend/.env

# .gitignore에 포함 확인
cat .gitignore | grep .env
```

### 8.5 API 접근 통제

모든 주요 API는 JWT 인증이 필수입니다. 인증 없이 접근 가능한 엔드포인트:
- `GET /health` — 서버 상태 확인
- `POST /auth/login` — 로그인
- `POST /auth/register` — 회원가입

> 💡 **운영 환경 권장**: 회원가입 API(`/auth/register`)를 관리자만 접근하도록 제한하거나, 초대 방식으로 변경하세요.

---

## 9. 모니터링 및 헬스체크

### 9.1 서버 상태 확인

```bash
# 헬스체크 API
curl http://localhost:8000/health

# 정상 응답
# {"status":"ok","app":"BAIKAL RPA AI"}
```

### 9.2 포트 사용 확인

```powershell
# Windows PowerShell
Get-NetTCPConnection -LocalPort 8000 -State Listen
Get-NetTCPConnection -LocalPort 5173 -State Listen

# Linux
ss -tlnp | grep -E '8000|5173'
```

### 9.3 프로세스 확인

```powershell
# Windows
Get-Process python | Select-Object Id, ProcessName, StartTime
Get-Process node | Select-Object Id, ProcessName, StartTime

# Linux
ps aux | grep -E 'uvicorn|node'
```

### 9.4 Docker 서비스 상태

```bash
# 전체 서비스 상태
docker-compose ps

# 리소스 사용량
docker stats

# 서비스별 로그
docker-compose logs -f --tail=100 api
docker-compose logs -f --tail=100 worker
```

### 9.5 자동화 실행 모니터링

```sql
-- 최근 실행 상태 확인
SELECT
  a.name AS automation_name,
  r.status,
  r.started_at,
  r.finished_at,
  SUBSTR(r.log, 1, 200) AS log_preview
FROM automation_runs r
JOIN automations a ON r.automation_id = a.id
ORDER BY r.started_at DESC
LIMIT 20;

-- 실패한 실행 조회
SELECT * FROM automation_runs WHERE status = 'failed'
ORDER BY finished_at DESC LIMIT 10;
```

---

## 10. 백업 및 복구

### 10.1 SQLite 백업

```bash
# 날짜별 백업
cp backend/baikal_rpa.db "backup/baikal_rpa_$(date +%Y%m%d_%H%M%S).db"

# 자동 백업 스크립트 (cron)
# 매일 새벽 2시 백업
0 2 * * * cp /path/to/backend/baikal_rpa.db /path/to/backup/baikal_rpa_$(date +\%Y\%m\%d).db
```

### 10.2 PostgreSQL 백업

```bash
# 논리 백업 (pg_dump)
docker-compose exec postgres pg_dump -U baikal baikal_rpa > backup_$(date +%Y%m%d).sql

# 물리 백업 (볼륨)
docker-compose stop postgres
docker run --rm -v baikal-rpa-ai_pg_data:/data -v $(pwd)/backup:/backup \
  alpine tar czf /backup/pg_data_$(date +%Y%m%d).tar.gz /data
docker-compose start postgres
```

### 10.3 복구

```bash
# SQLite 복구
cp backup/baikal_rpa_20260227.db backend/baikal_rpa.db

# PostgreSQL 복구
docker-compose exec -T postgres psql -U baikal baikal_rpa < backup_20260227.sql
```

### 10.4 업로드 파일 백업

```bash
# uploads 디렉토리 백업
tar czf uploads_backup_$(date +%Y%m%d).tar.gz backend/uploads/
```

---

## 11. 트러블슈팅

### 11.1 서버가 시작되지 않을 때

**증상**: `uvicorn` 실행 시 에러 발생

| 에러 메시지 | 원인 | 해결 |
|-------------|------|------|
| `ModuleNotFoundError: No module named 'aiosqlite'` | aiosqlite 미설치 | `pip install aiosqlite` |
| `Address already in use (port 8000)` | 포트 충돌 | 기존 프로세스 종료 후 재시작 |
| `No module named 'app'` | 잘못된 실행 경로 | `backend/` 디렉토리에서 실행 |
| `JWT_SECRET... validation error` | .env 파일 누락 | `.env` 파일 확인 |

**포트 충돌 해결 (Windows):**
```powershell
# 포트 8000 사용 프로세스 확인 및 종료
Get-NetTCPConnection -LocalPort 8000 -ErrorAction SilentlyContinue |
  ForEach-Object { Stop-Process -Id $_.OwningProcess -Force }
```

**포트 충돌 해결 (Linux/macOS):**
```bash
lsof -i :8000
kill -9 <PID>
```

### 11.2 프론트엔드 접속 불가

| 증상 | 원인 | 해결 |
|------|------|------|
| ERR_CONNECTION_REFUSED | 서버 미실행 | `npm run dev` 확인 |
| 흰 화면 | JS 빌드 에러 | 브라우저 콘솔 확인 |
| API 401 에러 | 토큰 만료/없음 | 재로그인 |

### 11.3 AI 응답이 안될 때

| 증상 | 원인 | 해결 |
|------|------|------|
| 500 에러 | OPENAI_API_KEY 미설정 | `.env` 확인 |
| Timeout | Ollama 서버 미실행 | `ollama serve` 실행 |
| 빈 응답 | 모델 미다운로드 | `ollama pull llama3` |
| 요금 초과 | OpenAI 크레딧 소진 | API 키 결제 확인 |

### 11.4 RPA 실행 실패

| 증상 | 원인 | 해결 |
|------|------|------|
| Playwright 에러 | 브라우저 미설치 | `playwright install chromium` |
| 파일 못 찾음 | file_path 오류 | 업로드 경로 확인 |
| 권한 에러 | 파일 권한 부족 | `uploads/` 디렉토리 권한 확인 |
| 타임아웃 | 대상 사이트 차단 | selector/URL 확인 |

### 11.5 DB 관련 에러

| 에러 | 원인 | 해결 |
|------|------|------|
| `OperationalError: database is locked` | SQLite 동시 접근 | 서버 단일 프로세스 확인 |
| `relation does not exist` | 테이블 미생성 | 서버 재시작 (auto-create) |

---

## 12. 운영 체크리스트

### 12.1 초기 배포 체크리스트

- [ ] `.env` 파일 설정 완료
- [ ] JWT_SECRET 랜덤 값으로 변경
- [ ] OPENAI_API_KEY 설정 (또는 Ollama 구성)
- [ ] DB 비밀번호 강력한 값으로 설정
- [ ] CORS_ORIGINS에 운영 도메인 설정
- [ ] 관리자 계정 생성 완료
- [ ] HTTPS (SSL/TLS) 적용
- [ ] 방화벽 규칙 설정 (8000, 5173 포트)
- [ ] 백업 스케줄 설정
- [ ] 모니터링 알림 구성

### 12.2 정기 운영 체크리스트

| 주기 | 작업 |
|------|------|
| **매일** | 헬스체크 API 확인 |
| **매일** | 실패한 자동화 실행 확인 |
| **매일** | DB 자동 백업 확인 |
| **매주** | 시스템 리소스(CPU/RAM/디스크) 확인 |
| **매주** | 로그 파일 용량 확인 및 정리 |
| **매월** | OpenAI API 사용량/비용 확인 |
| **매월** | 보안 패치 및 의존성 업데이트 확인 |
| **분기** | JWT_SECRET 교체 (선택) |
| **분기** | DB 비밀번호 변경 (선택) |
| **분기** | 미사용 데이터 정리 (문서, 실행 기록) |

### 12.3 서비스 업데이트 절차

```bash
# 1. 소스코드 업데이트
git pull origin main

# 2. 백엔드 의존성 업데이트
cd backend
pip install -r requirements.txt

# 3. 프론트엔드 의존성 업데이트
cd ../frontend
npm install

# 4. 서비스 재시작
# 로컬: 터미널에서 Ctrl+C 후 재시작
# Docker:
docker-compose up -d --build
```

---

## 부록 A: 주요 포트 목록

| 서비스 | 포트 | 프로토콜 |
|--------|------|----------|
| FastAPI 백엔드 | 8000 | HTTP |
| React 프론트엔드 | 5173 | HTTP |
| PostgreSQL | 5432 | TCP |
| Redis | 6379 | TCP |
| Ollama | 11434 | HTTP |

## 부록 B: 유용한 명령어 모음

```bash
# 서버 API 문서
open http://localhost:8000/docs

# 전체 사용자 목록 (SQLite)
sqlite3 backend/baikal_rpa.db "SELECT email, name, role FROM users;"

# 최근 AI 생성 문서 5건
sqlite3 backend/baikal_rpa.db "SELECT title, doc_type, created_at FROM documents ORDER BY created_at DESC LIMIT 5;"

# 자동화 실행 성공률
sqlite3 backend/baikal_rpa.db "SELECT status, COUNT(*) FROM automation_runs GROUP BY status;"

# 오늘 자동화 실행 수
sqlite3 backend/baikal_rpa.db "SELECT COUNT(*) FROM automation_runs WHERE DATE(started_at) = DATE('now');"
```

---

> **© 2026 BAIKAL AI. All rights reserved.**
